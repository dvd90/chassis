{
  "name": "chassis-docs",
  "version": "1.0.0",
  "private": true,
  "description": "Builds the Chassis documentation site from docs/**/*.md.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs --check"
  },
  "devDependencies": {
    "marked": "^15.0.0"
  }
}
